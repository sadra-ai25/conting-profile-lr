services:
  redis:
    image: redis:7.0-alpine
    ports:
      - "6379:6379"

  profile_count:
    build: .
    image: profile_count
    volumes:
      - ./.env:/app/.env
      - ./src/ai/weights:/app/src/ai/weights
      - .:/app
      - ./src:/app/src
      - ./outputs:/app/outputs
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    ports:
      - "8001:8001"
    env_file: .env
    depends_on:
      - redis
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]